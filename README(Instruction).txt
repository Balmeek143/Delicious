The project is made in react js. All the modules that are used in this code are listed in dependencies section of package.json. To get all the modules that are installed in this code, You 
have to run "npm i" in the project directory. Then you can execute the code by using command "npm start" in the project directory.
